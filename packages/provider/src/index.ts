// Placeholder — types arrive in the next commit. Each provider package will
// import the `ModelProvider` interface and the shared input/output types from
// here; helpers default to per-provider copies rather than abstracted exports.
export {};
